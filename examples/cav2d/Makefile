
TARGET = cav2d.exe
ROOT := ../..

FLAGS += -O3 -march=native -fopenmp
# FLAGS += -O3 -march=native
# FLAGS += -g
# FLAGS += -pg
FLAGS += -DFLOAT_TYPE=double

OUTPUT += vtkoutput

include $(ROOT)/make.mk
